from wxdata.fems.fems import get_raws_sig_data


df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('oscc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('oncc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('nwcc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('nrcc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('gbcc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('swcc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('rmcc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('eacc', 15, 'Y', None)

df_data, df_climo_avg, df_climo_min, df_climo_max, dates = get_raws_sig_data('sacc', 15, 'Y', None)